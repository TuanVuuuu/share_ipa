share-ipa.vunt.site {
	tls internal
	reverse_proxy localhost:3000
}

# LAN HTTP — tải nhanh trong mạng nội bộ (Android OK).
# .env: LAN_BASE_URL=http://<IP-LAN>:3080
http://:3080 {
	reverse_proxy localhost:3000
}
